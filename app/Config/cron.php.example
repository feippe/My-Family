<?php
/**
 * Configuración del cron de recordatorios.
 * Este archivo no se usa en el script CLI (cron/reminders.php lo lee todo
 * directo del resto de la config). No necesitás crear este archivo.
 *
 * El cron que tenés que agregar en cPanel → Cron Jobs (cada 5-10 minutos):
 *
 *   /opt/cpanel/ea-php81/root/usr/bin/php -q /home/feippec2/familia.feippe.com/cron/reminders.php
 *
 * El runner envía dos recordatorios a todos los participantes de cada evento:
 *   - 24 horas antes de la fecha/hora de inicio
 *   - 30 minutos antes de la fecha/hora de inicio
 */
return [];
